   <!-- <div class="slider">
      <img src="https://img.icons8.com/material-rounded/48/ffffff/delete-sign.png"/ id="close">
      <div class="slideContent">
         <% if(cart.length > 0){%>
             <% for(let product of cart) {%>
               <div class="cartContent">
                  <img src="<%= product.image %>" alt="">
                  <div class="priceAndAll">
                     <h3><%= product.title %></h3>
                     <div class="left">
                        <h4>Rs <%= product.price %></h4>
                        <h4 class="quan">Quantity: <%= product.quantity %></h4>
                     </div>
                  </div>
               </div>
            <%} %>
            <div class="linebreak"></div>
            <h1 class="total">Total : <%= totalPrice %></h1>
            <% }else{%>
               <h4>Nothing To Display</h4>
            <% } %>  
      </div>
   </div> 
    -->